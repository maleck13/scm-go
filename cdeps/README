This is the version of libgit libgit2-0.23.4 .
This is built as a static c artifact and then the go code statically links against it producing a single binary
 
To build the .a (archive) you will need 
cmake
pkg-config